---
trigger: model_decision
description: This rule should always be applied whenever significant logic/code is changed/added within the application's codebase.
---

# Unit Test Rules

- Upon Significant codebase changes, always run tests. 
- Tests should always be updated to reflect the current state of the app.
- Tests should always be introduced if new logic is also introduced.
- Tests should be exhaustive and should tests every function in the codebase.
